#include <iostream>
using namespace std;
int main(){
// sizeof operator is used to find the size of a data type or variable in bytes
 int  age =25;
cout<<"My age is "<<age<<" and its size is "<<sizeof(age)<<" bytes"<<endl;

  return 0;
}
